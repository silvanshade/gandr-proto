# Guards — the do-not-reopen ledger

Declined halves with their reversal conditions, dissolved forks, withdrawn claims, and name-collision warnings.
Everything here has been litigated at least once; a wrong rejection is silent and compounding, so each entry names the exact condition under which it may be revisited.
The concordance at the end maps the retired records' letter-number codes to the current named anchors.

## Declined, with reversal conditions

### The declined completeness half

The layout calculus's **faithfulness/completeness theorem** (congruent-iff-equal-realization) is declined. gandr consumes soundness only; certificates are compared at replay-equivalence; the engine's normal-form test is a decidable under-approximation, and a normal-form-equal, replay-divergent pair is a **kill signal**, not a hole to close by theorem.
The expensive part of a faithfulness proof was located and shown presentational (an artifact of representing a bracketing the arena does not represent), so the work is not skipped because it is hard.

> **The reversal condition**: if the normal-form test is ever promoted from an under-approximation to a **decision procedure** for replay-equivalence, completeness becomes load-bearing and faithfulness returns to the arc.
> Note this is the decidability ceiling seen from the other side: promoting the under-approximation globally asks for a theorem that is false in general.

If it ever fires, the routes are catalogued (decide by focusing over skew structure; decide by normalization at the multicategory layer; dissolve by the simple-connectivity coherence theorem; measure by arity approximation) — none is scheduled, and none should be read as an open obligation.
The corresponding residues (symmetric group in the groupoid alphabet; transformation monoid in the directed one) are likewise **not owed**.

### The general distributive-coherence family

Do **not** name the general distributive-coherence family as an open obligation.
It is a **discharged spike** with a bounded, named residue that no consumer reads: the hierarchy dissolves by the rigid-coherence theorem, the content-carrying generators are proved directly, and the unattempted diagrams (the product-symmetry hexagon; mixed symmetry/distributor diagrams) reduce to the Coxeter word problem the completeness decline already covers.
Naming it as open invites "why has this not been proved?", which is the wrong question.

### The free-bifibration STOP

Freely adjoining fibrational structure (the universal repair when a fibrational axiom fails) is an owner STOP whose **entry condition is a factorization-preorder check on the seam category** — without that property, 2-cell equality in the free construction is undecidable.
Content-addressed first-order syntax plausibly qualifies; testable, not assumed.

### The modality decision

No internal cohesion; no surface modality.
Revisit **only** if: the code universe stops being a Tarski code family (globality bites); a construction needs display on open terms; or the variance layer is built.
Adopting a crisp-variable discipline costs every binder; the capabilities have other routes.

### Horizontal-composition surface sugar

Two simultaneous rewrite arguments in the boundary language are declined because they denote horizontal composition, whose two sequential readings agree only up to interchange.
The principled future semantics exists: accept exactly on **disjoint positions**, where the two readings are shift-equal.
Do not accept it any earlier or any wider.

### Clones as the arena's morphism class

The arena's directed generalization is **built as a factorization system and characterized as the clone**; rebuilding the arena with clone-shaped morphisms is declined because it changes what a morphism _is_ (every existing lemma re-examined) where the split is an extension (nothing that typechecks today changes).

### Nominal decoration

Decorating flags by names is formally possible and declined: sharing should become a **wire**; a name decoration would entrench the nominal encoding at exactly the point the design says should become structural.
The nominal _type-theory_ aim (nullary parametricity as name abstraction) is a different thing and is adopted — the two are genuine alternatives for the binder story, and the open question is which, not whether.

## Dissolved forks — do not re-litigate

* **Planar versus groupoid** is not a fork: ordering is a section of the quotient, never a planarization; at the as-built layer there is no symmetry to give up, and at the design layer the question is settled by the identification-sorting test (presentation symmetry free; automorphism symmetry to canonicalization soundness).
* **Tight versus loose** is a stratification, not a three-way architectural fork: the loose object is the ambient hom, the tight object is the modality over it, and each object sits at a layer rather than the design choosing a home.
* **The monad-composition placement** ("compose the derivation arity with the term arity") is interchange, on which the witness ruling has already ruled; its lax form at the certificate layer is settled, its strict form at the shape layer is the scheduled duoid equation, and the only named payoff of a genuinely composite doctrine-and-term monad (a nerve theorem for the doctrine itself, making _certificates_ univalent) is reachable by the universe-presentation route without it.
* **`Web` as the many-to-many arity**: a web is a forest — many-in, one-out per component — the operad-of-trees arity; the properadic arity is a _graph_.
  Its surviving contribution is the witness discipline.
* **Full multi-globularity** (many-to-many coboundaries at every dimension) overshoots: multi-arity is needed at exactly the base dimension; the trigger to revisit is a genuine (non-singleton) multi-sum family, which is measured and currently singleton.

## Withdrawn claims — tombstones

Each was asserted in a retired record and is withdrawn with its reason; do not re-import.

| withdrawn claim                                                                                      | why                                                                                                                                                                                               |
| ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| "the cell shape must be simply connected" (the dioperad commitment) as a _substrate_ restriction     | superseded by the full-rung ruling; hom-finiteness is free on the downward rung with no graph-shape hypothesis; rectifiability and Σ-freeness were bought for consumers that no longer spend them |
| "Σ-freeness is the single load-bearing criterion"                                                    | re-scoped: true of the ordered representation by construction, false of the rung constitutively, load-bearing for neither the nerve (arities) nor the arities themselves                          |
| "a set-level cartesian monad for gandr's shape exists at the circuit rung"                           | the strong-cartesianness claim routes through the ∞/groupoid framework and does not transport to `Set`, where the monad takes honest coinvariants; the arities claim is the `Set`-level survivor  |
| "properads, with connected composition only, are the proved ceiling; PROPs remain uncovered"         | refuted: the circuit-algebra nerve covers disconnected graphs and transports to wheeled props                                                                                                     |
| "restrict to dioperads, therefore give up many-out"                                                  | dioperads have the same colour set as properads; what the higher rungs add is reconvergence, disconnection, and wheels — not arity                                                                |
| "the naive root ladder organizes the symmetry axis"; "a HIT-free escape reaches the symmetry rung"   | both withdrawn in the consolidation; the site organizes the strata, and decidability arrives from edge-determination                                                                              |
| "planarity does not cost cocommutativity"                                                            | retracted; the argument was a non sequitur and the sources point the other way                                                                                                                    |
| "perturbation is local" as a four-decision design principle                                          | refuted as stated; salvaged as bounded address-map sensitivity, explaining content-addressed chunking only                                                                                        |
| "the certificate normal form decides replay-equivalence" (in either direction beyond the stated iff) | the converse is constructibly false in the shipped code                                                                                                                                           |
| "gandr's shift equivalence is the source's shift equivalence / is Lévy equivalence"                  | it is the sequential-independence restriction of the first and the _reversible_ refinement of the second; neither theorem may be cited as though the relations coincided                          |
| "the residual theory refuses to compose treks"                                                       | refuted — the cited passage concerns residuation, not composition                                                                                                                                 |
| "rigidity means the objects have trivial automorphisms"                                              | false at every rung above one-in/one-out; rigidity is a property of the representation                                                                                                            |
| "decorated objects inherit substrate theorems _because they form a Feynman category_"                | citation withdrawn (wrong formalism for this carrier); the conclusion is mechanized directly — decoration by structure-on-colours is a carrier parameter                                          |
| "the region-indexed certification device approximates multiple directions of higher-dimensionality"  | withdrawn; it tracks _depth_, not _direction_ — the direction answer is the native directed former with invertibility as overlay                                                                  |
| "monadic composition of the two arities is needed for the join"                                      | ill-typed as posed (the kits are bases, not endofunctors of one category); what is real is decomposition _inside_ the term monad                                                                  |

## Name collisions — read the definition, not the section title

Four collisions were caught in one pass and one shipped before it was checked; the lens that works costs minutes.

* **"simply connected"** names three different things: a condition on cell-shape _objects_ (deliberately dropped); a computed property of a _coherence complex_ (which you do not choose — it decides whether coherence holds, and applies to both universe structures); and partition-complex _connectivity_ (which you want high, since it lowers the arity at which coherence data is determined).
  The mode distinction that keeps them apart: **presented** structure has a coherence question; **defined** operations have equations to prove, and no coherence theorem addresses them.
  Canonicalization is what would move the pasting structure from defined to presented — that is the item to watch.
* **"polarity"**: the pretype-theory line's polarities are _variance modalities_; gandr's are CBPV producer/consumer via the palette orientation.
  Different axes; the useful relation is that the first is a worked solution to the variance cost gandr prices separately.
* **"Feynman"**: see the naming hazard in the main document — two different objects under one word.
* **"Conduché"**: this one _survived_ the check — the measured cellular condition is a definitional match with the discrete Conduché condition, not a name match.
* **"arity"**: monads-with-arities (nerve side) versus arity monads (carrier side) come apart exactly at gandr's rung; every claim must say which sense it uses.
* **"hereditary"**: the Koszul line supersedes the polytope/cubical-Feynman technique, not the hereditary condition; hereditariness — many-to-many maps decomposing uniquely as tensor products of many-to-one maps, the axis whose _failure_ distinguishes props and wheeled structures from operads — is **not decided anywhere in the corpus** and stays an open lead, not a settled question.
* **"Rigid"**: two structures share the name — the canonicalization record on a setoid (`Gandr.Rigid`) and the extent-preserving offset-fixed arena map class (`Gandr.Arena.Structure.Rigid`).
  The shared diagnosis (the representation carries structure the semantics cannot see, and rigidity certifies it does not leak — bracketing in one, ordering in the other) is why the name recurs; a rename decision is owed **before a third `Rigid` appears**.

## Mode and posture rules, standing

* **The generality ruling** (owner, binding): the substrate carries full circuit-algebra generality; restrictions are gandr-side static analysis; a carrier notion that cannot express what the shape layer provides is wrong, not scoped.
* **Build the residual now**: implement remaining pieces as they occur; if a piece will not close, parameterize a module by its assumptions so what is assumed is in the signature.
  Three residuals taken rather than deferred each exposed a structural defect; a deferred residual is a claim that nothing depends on it, and that claim has been false three times running.
* **Characterize before building**: say what a structure is categorically and lay the instances out first; an instance that cannot be filled is an obligation nobody had listed.
* **A refutation resting on a fact about us is a representation finding, not a refutation** — and a fact about us that is _mechanized_ is the strongest warrant available, not the weakest.
* **A price quoted against a superseded presentation is not evidence about the current cost** — re-quote before spending.
* **Geometry as motivation is free; geometry as primitive is priced** (the trusted-surface criterion).
* **Adversarial findings against a published paper are tensions with a resolution path**, not discoveries of error, unless proved otherwise; carry each source's publication status with its claims.

## Code concordance

The retired records used letter-number codes; their load-bearing content now lives at named anchors.
This table exists so old notes remain interpretable — do not use the codes in new writing.

| retired code                                                 | current home                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M1 / C1–C6 (consolidated proposal decisions and commitments) | M1 → "one construction, two arity bases" (split: the construction survives as base-change; the cartesianness licence is replaced); C1/C5 → superseded by "the substrate is the full circuit-algebra rung"; C2 → "the nerve at the circuit rung"; C3 → "ordering is a section"; C4 → "the parallel direction stays symmetric"; C6 → the normal-form contract in "the certificate algebra" |
| M2–M22                                                       | dissolved into the named decisions table of the main document; the withdrawn ones are tombstoned above                                                                                                                                                                                                                                                                                   |
| D1–D60 (the substrate and doctrine delta lists)              | executed by this spec reboot where they were commitments; the construction/spike items are in [[roadmap]]                                                                                                                                                                                                                                                                                |
| F2.x / F3.x (the circuit-algebra findings log)               | folded into the main document's substrate, arity, and symmetry sections and [[carrier]]                                                                                                                                                                                                                                                                                                  |
| S-1 … S-13 and S-7a…e (the old spike queues)                 | superseded by [[roadmap]]'s spike table                                                                                                                                                                                                                                                                                                                                                  |
| O1–O4 / O1⇝–O4 (univalence obligations)                      | "sound / full / sectioned / core-coincident" in [[directed-univalence]] and the main document                                                                                                                                                                                                                                                                                            |
| P-a … P-d, P-a′, P-a″ (temporal predictions)                 | P-a″ is "no primitive without priced trusted surface"; the rest are folded into the falsifier list                                                                                                                                                                                                                                                                                       |
| Ł1–Ł4 (the directed equipment ladder)                        | the equipment inventory in [[directed-univalence]]                                                                                                                                                                                                                                                                                                                                       |
| W-numbers / Q-numbers (intake and register items)            | not carried; where load-bearing, their content is stated in place                                                                                                                                                                                                                                                                                                                        |

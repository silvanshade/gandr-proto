# gandr design corpus

Stub index for the gandr documentation corpus.
The corpus tree is grown by the reboot build-out; registered nodes are hashed in `MANIFEST.yml` (discipline: `docs/KNOWLEDGE.md`).

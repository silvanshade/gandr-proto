//! The gandr toolchain driver.
//!
//! Stub entry point: the REPL, script-runner, and
//! `tui`/`lsp`/`mcp`/`fmt`/`build` faces land with the crates they wrap (see
//! the parked dependency surface in `Cargo.toml`).

/// Program entry point.
///
/// # Contract
/// - provides: an empty driver body; exits successfully with no output.
/// - panics: none.
fn main()
{
}

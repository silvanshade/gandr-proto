#:schema https://www.schemastore.org/cargo.json
#:tombi toml-version = "v1.0.0"

[package]
name = "gandr-theory-cell-complexes-tools"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Test-facing second inhabitant of the cell-alphabet trait: a first-order toy alphabet with nesting commands, plus five adversarial wrappers that each break one law the engines above the substrate rely on"
license.workspace = true
publish.workspace = true
autotests = false

[[test]]
name = "cell_complexes_tools"
path = "tests/lib.rs"

[dependencies]
gandr-theory-cell-complexes.workspace = true

[features]
default = []
full = []

[lints]
workspace = true

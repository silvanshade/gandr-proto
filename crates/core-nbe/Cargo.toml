#:schema https://www.schemastore.org/cargo.json
#:tombi toml-version = "v1.0.0"

[package]
name = "gandr-core-nbe"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Normalization by evaluation for the gandr core: the glued semantic domain, evaluation, readback, and the definitional-equality pipeline that decides conversion"
license.workspace = true
publish.workspace = true

[dependencies]
# The core CBPV terms and types this engine evaluates, compares, and reads back.
gandr-core-term.workspace = true
gandr-kernel-strata.workspace = true

[features]
default = []
full = []

[lints]
workspace = true

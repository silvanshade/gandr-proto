; gandr pre-lowered corpus fixture (byte anchor; sequent-machines exit gate)
; source: pathological/kernel-admission-boundary.gandr
; b3sum: cc7f0c3dcee694fb558b484003204246457272fbf3342a101548b90ea72c7374
; lowering: gandr_surface_engine::lower::lower_source_total
; items: 5

(v
  (s "the value zone"))

(v
  (pair
    (var "greeting")
    (var "greeting")))

(v
  (thunk
    gomega
    (abs
      "x"
      none
      (ret
        (var "x")))))

(v
  (n
    (u32 1)))

(c
  (ret
    (var "greeting")))

// Failure golden: a package's grade IS its payload thunk's grade, so a payload
// of any other shape leaves the package with no grade to read. Formation
// refuses it rather than repairing it into something checkable.
def bad : package [ T ] Integer;
def bad = ret 1;

bad

//@ mode: shell
//@ expect-shell-error: a package payload must be a graded thunk

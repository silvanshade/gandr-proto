#:schema https://www.schemastore.org/cargo.json
#:tombi toml-version = "v1.0.0"

[package]
name = "gandr-runtime-effects"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Headless host-effect runtime for gandr: native Exec/Fs/Proc/Env handlers over the host-effect seam, driven by the L machine (run_comp_with_host)"
license.workspace = true
publish.workspace = true

[dependencies]
gandr-core-checker.workspace = true
gandr-core-sequent.workspace = true
thiserror = { workspace = true }

[features]
default = []
full = []

[lints]
workspace = true

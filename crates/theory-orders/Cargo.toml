#:schema https://www.schemastore.org/cargo.json
#:tombi toml-version = "v1.0.0"

[package]
name = "gandr-theory-orders"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Self-contained order-maintenance structure (a total order with O(1) comparison) for the gandr incremental pipeline (A2)"
license.workspace = true
publish.workspace = true
autotests = false
autobenches = false

[[test]]
name = "oracle"
path = "tests/lib.rs"

[[bench]]
harness = false
name = "order"

[features]
default = []
full = []

[lints]
workspace = true

[dependencies]
thiserror.workspace = true

[dev-dependencies]
criterion.workspace = true
proptest.workspace = true

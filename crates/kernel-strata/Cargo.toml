#:schema https://www.schemastore.org/cargo.json
#:tombi toml-version = "v1.0.0"

[package]
name = "gandr-kernel-strata"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "The minimal certified kernel's level oracle -- canonical {0, +1, max} universe levels with an evidence-returning order oracle, plus landmark-poset admission and entailment by Bezem-Coquand loop-checking (ADR-78; docs/gandr/spec/implementation.md \"The trusted base\")"
license.workspace = true
publish.workspace = true

# TCB dependency wall (kernel-boundary.md section 2): a gandr-kernel-* crate
# depends only on other gandr-kernel-* crates and core/alloc. Dev-dependencies
# are exempt -- test code is not shipped trust.
[dependencies]

[dev-dependencies]
proptest.workspace = true

[features]
default = []
full = []

[lints]
workspace = true

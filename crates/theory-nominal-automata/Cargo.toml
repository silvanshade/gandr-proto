#:schema https://www.schemastore.org/cargo.json
#:tombi toml-version = "v1.0.0"

[package]
name = "gandr-theory-nominal-automata"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Generic nominal atom substrate -- a sort-tagged Atom<S> and a monotone Gensym<S> allocator with an atom-vs-variable sort boundary -- consolidating gandr's machine-minted name-spaces (ADR-41)"
license.workspace = true
publish.workspace = true

[features]
default = []
full = []

[lints]
workspace = true

[dev-dependencies]
proptest.workspace = true
